arkanoid game

# standards
* use `yarn` for all npm related commands
* use proper typescript types
* add unit tests for all logic
* use vite for testing
* use eslint for linting
* install packages if they are missing

